// e2e-тесты гоняются на in-memory репозитории — без поднятой PostgreSQL.
// Драйвер читается в app.module.ts синхронно при импорте, поэтому переменную
// окружения нужно выставить раньше, чем Jest подтянет тестовые модули.
process.env.DATABASE_DRIVER = 'memory';
